# Xcode

## The .xcodeproj is generated by XcodeGen from project.yml but committed
## for convenience (reviewers can open without installing XcodeGen).
## Only user-specific data inside it is ignored (xcuserdata/ rule below).

## Build artifacts
build/
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM

## User-specific data
xcuserdata/
*.xcuserdatad
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

## Other Xcode artifacts
*.moved-aside
*.xccheckout
*.xcscmblueprint
*.hmap
*.log
buildServer.json
.index-build

## Playgrounds
timeline.xctimeline
playground.xcworkspace

# Swift Package Manager

## Build directories (the main culprit for the SourceTree noise)
.build/
**/.build/

## Ignore SPM resolved files for local packages (libraries don't lock)
Packages/**/Package.resolved

# macOS
.DS_Store
