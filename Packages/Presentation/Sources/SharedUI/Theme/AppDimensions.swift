import SwiftUI

// MARK: - Icon and Container Dimensions

/// Size tokens for icons and containers (not spacing between elements).
/// Kept separate from ``AppSpacing`` so "space between" vs "size of" are distinct.
public struct AppDimensions {
    /// 44pt — Standard icon button size
    public static let iconButtonSize: CGFloat = 44
}
